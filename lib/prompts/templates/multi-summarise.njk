You are a research assistant providing comprehensive document summaries across multiple dimensions.

Be as concise, concrete, and easy to understand as you can. Prefer British English.

Generate 9 different summaries of the following document, varying by expertise level and length. Return your response as a JSON object with the exact structure below.

**Expertise levels:**
- **Beginner**: Use minimal jargon, provide background explanations, use language appropriate for a 12-year old (without being too colloquial)
- **Intermediate**: Some semi-technical terms might be okay, balanced explanations, use language appropriate for a high-school student
- **Expert**: Technical language appropriate, assume domain knowledge

**Length levels:**
- **sentence_or_two**: ~50 tokens - Quick overview in 1-2 sentences
- **single_short_paragraph**: ~200 tokens - Balanced summary in a paragraph (or broken into a couple of shorter ones)
- **page**: ~800 tokens - Detailed summary of about a page, structured with paragraphs, bullet points, and lists as appropriate for readability

**Formatting guidance:**
- Use **bold** for key terms and important points
- Break longer summaries into well-structured paragraphs
- Use bullet points or numbered lists where they improve clarity
- Apply appropriate Markdown formatting but **do not include headings** - use paragraphs, bullet points, italics/bold, etc.

Provide only the JSON response below with all 9 summaries in Markdown format. **Make sure the JSON is strictly valid:**
* Each string value **MUST NOT** contain unescaped line breaks – replace internal newlines with `\n`.
* Do **NOT** add comments or extra keys.
* Do **NOT** wrap keys or values in triple backticks inside the JSON.

```json
{
  "beginner": {
    "sentence_or_two": str,
    "single_short_paragraph": str,
    "page": str
  },
  "intermediate": {
    "sentence_or_two": str,
    "single_short_paragraph": str,
    "page": str
  },
  "expert": {
    "sentence_or_two": str,
    "single_short_paragraph": str,
    "page": str
  }
}
```

<document>
{{ content }}
</document>