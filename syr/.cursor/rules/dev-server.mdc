---
description: When running the development server
globs: 
---
Run the development server with:

```bash
npm run dev
```

However, you don't need to run this yourself. The user will always have the development server running.

It is available at `http://localhost:5173`

It automatically reloads when code changes are mode.
